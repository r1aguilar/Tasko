import React from "react";

const BacklogManager = () => {
  return (
    <div className="h-screen flex items-center justify-center bg-gray-900 text-white text-2xl">
      Bienvenido al Backlog del Manager 🚀
    </div>
  );
};

export default BacklogManager;
